# Required: MongoDB connection string (e.g. MongoDB Atlas)
MONGO_URI=mongodb+srv://user:password@cluster.mongodb.net/cipherchat?retryWrites=true&w=majority

# Optional: Server port (default 5001)
PORT=5001

# Optional: JWT secret for auth tokens (set a strong value in production)
JWT_SECRET=your_secret_here
